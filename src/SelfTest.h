#pragma once

namespace d3d {

// Самотест ядра Director 3D: кривые, дорожки, документ, транспорт, разбор WAV,
// отмена и файл проекта. Запускается флагом `--self-test` и НЕ трогает ни окно,
// ни OpenGL — поэтому гоняется в CI на обычном раннере без видеокарты.
//
// Возвращает код возврата процесса: 0 — все проверки прошли, 1 — что-то упало
// (подробности печатаются в stdout и в лог).
int RunSelfTest();

} // namespace d3d
