#pragma once
#include <chrono>
#include <optional>
#include <string>
#include <vector>

#include "anim/AnimationDocument.h"
#include "render/VideoWriter.h"
#include "sage/render/Framebuffer.h"

class Scene;
struct LightingEnvironment;

namespace d3d {

class StageRenderer;

// Каталог вывода по умолчанию: <домашняя папка>/Director3D/render.
//
// Раньше по умолчанию стоял относительный путь "render" — то есть «рядом с
// программой». На Linux из каталога сборки это работало, на Windows же
// программа лежит там, куда её распаковали: в Program Files или в Загрузках
// внутри архива. В первом случае каталог просто не создаётся (нет прав), во
// втором готовый ролик оказывается внутри временной папки. И в обоих человек
// не знает, где искать результат. Домашняя папка есть всегда, доступна на
// запись всегда и находится без объяснений.
std::string DefaultRenderDir();

// ---------------------------------------------------------------------------
// SequenceExporter — вывод готового ролика в файлы.
//
// Два формата вывода:
//   • MP4 (H.264) — готовый ролик, который можно сразу смотреть и отправлять.
//     Кадры уходят кодировщику потоком, без промежуточных файлов (см.
//     VideoWriter); при наличии звуковой дорожки она подмешивается в тот же файл;
//   • секвенция PNG — кадр в файл вида name_00001.png. Без потерь, принимается
//     любым монтажным пакетом, и работает там, где нет ffmpeg.
//
// Экспорт идёт ПО КАДРАМ и синхронно, кадр за кадром: для каждого номера кадра
// время ставится точно (time = frame / fps), документ применяется в режиме
// перемотки (позы клипов встают на своё место независимо от истории), сцена
// рисуется в буфер нужного разрешения и читается в файл. Поэтому результат не
// зависит ни от частоты кадров экрана, ни от того, тормозила ли машина, — в
// отличие от «записи с экрана».
//
// Экспорт растянут по кадрам приложения (Step вызывается из главного цикла),
// чтобы окно не висело намертво на длинном ролике и показывало прогресс.
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// RenderQueue — список заданий рендера, выполняемых подряд.
//
// Зачем. Один рендер блокирует работу: пока идёт экспорт, инструментом
// пользоваться нельзя. Значит, снимать три плана — это три раза дождаться
// окончания и три раза руками переставить настройки. Очередь превращает это в
// «поставил три задания и ушёл».
//
// Очередь хранит НАСТРОЙКИ, а не готовые кадры: сцена и документ берутся
// текущие на момент старта задания. Это осознанно — иначе очередь пришлось бы
// делать снимком всего проекта на каждое задание, а типичный случай как раз
// обратный: тот же проект, разные диапазоны и разрешения.
// ---------------------------------------------------------------------------
class SequenceExporter {
public:
    // Куда пишем результат.
    enum class Format {
        Mp4,          // готовый ролик H.264 (нужен ffmpeg)
        PngSequence,  // кадр = файл, работает всегда
    };

    struct Settings {
        Format OutputFormat = Format::Mp4;
        std::string OutputDir = "render";
        std::string BaseName = "frame";
        // Качество H.264 (CRF): 18 — визуально без потерь, 23 — обычное.
        int Quality = 18;
        bool IncludeAudio = true; // подмешать звуковую дорожку проекта в MP4
        int Width = 1920;
        int Height = 1080;
        // Сглаживание накоплением: кадр снимается Samples раз с микросдвигом
        // проекции, результаты усредняются. 1 — выключено. Это НЕ экранный
        // фильтр: усреднение убирает и лесенку, и мерцание тонких деталей, и
        // шум шейдеров — ценой линейного роста времени рендера. В чистовом
        // выводе время есть, в интерактивном вьюпорте его нет (там FXAA).
        int Samples = 1;
        float StartTime = 0.0f;
        float EndTime = 0.0f;   // 0 — до конца ролика
        int CameraId = -1;      // с какой камеры снимаем
        // Сколько кадров писать за один кадр приложения: больше — быстрее
        // экспорт, но интерфейс отзывается реже.
        int FramesPerStep = 1;
    };

    // Готовит экспорт: создаёт каталог, считает диапазон кадров. err — причина
    // отказа (нет камеры, каталог не создать). Возвращает false, не начав.
    bool Begin(const Settings& settings, const AnimationDocument& doc, Scene& scene, std::string& err);

    // Пишет очередную порцию кадров. Возвращает false, когда экспорт завершён
    // или прерван; подробности — в Failed()/Error().
    bool Step(Scene& scene, StageRenderer& renderer, AnimationDocument& doc);

    void Cancel();

private:
    // Снимает кадр несколько раз с микросдвигом и усредняет прямо в m_frameBuffer.
    // false — камера пропала посреди экспорта.
    bool RenderAccumulated(Scene& scene, StageRenderer& renderer, const LightingEnvironment& env,
                           int cameraId,
                           int samples);

public:

    bool Active() const { return m_active; }
    bool Failed() const { return m_failed; }
    const std::string& Error() const { return m_error; }
    int CurrentFrame() const { return m_current; }
    int TotalFrames() const { return m_total; }
    float Progress() const { return m_total > 0 ? (float)m_current / (float)m_total : 0.0f; }

    // Сколько идёт экспорт и сколько ещё ждать, в секундах. Без этих двух чисел
    // прогресс-полоса отвечает только на «сколько сделано», а человек у экрана
    // спрашивает «уходить мне или нет» — и, не получив ответа, решает, что
    // программа повисла.
    //
    // Оценка считается по уже снятым кадрам, поэтому первые пару кадров она
    // отсутствует (RemainingSeconds вернёт -1): врать «осталось 0 секунд» на
    // старте хуже, чем честно промолчать.
    float ElapsedSeconds() const;
    float RemainingSeconds() const;

    const std::string& OutputDir() const { return m_settings.OutputDir; }
    // Человекочитаемый путь результата — то, что показывается в статус-баре.
    const std::string& ResultPath() const { return m_resultPath; }

private:
    Settings m_settings;
    // Буфер рендера живёт на всё время экспорта и освобождается по его
    // окончании: пересоздавать текстуры на каждый кадр — сотни лишних
    // аллокаций в GPU-памяти подряд, держать вечно — занятая VRAM без дела.
    std::optional<Framebuffer> m_target;
    // Кодировщик и буфер чтения кадра. Буфер переиспользуется: выделять
    // мегабайты на каждый кадр — это заметная нагрузка на аллокатор.
    VideoWriter m_video;
    std::vector<unsigned char> m_frameBuffer;
    // Накопитель для сглаживания. Сумма в float: складывать сотни выборок в
    // байтах — это потерять всё, ради чего сглаживание затевалось.
    std::vector<float> m_accum;
    std::string m_resultPath;
    bool m_active = false;
    bool m_failed = false;
    std::string m_error;
    int m_current = 0;
    int m_total = 0;
    int m_firstFrame = 0;
    float m_fps = 24.0f;
    // Отсчёт для оценки времени. steady_clock, а не system_clock: перевод часов
    // посреди рендера не должен превращать оставшееся время в отрицательное.
    std::chrono::steady_clock::time_point m_startedAt{};
};

// Одно задание очереди: имя для списка и полные настройки экспорта.
// Не RenderJob — это имя занято пакетным рендером из командной строки
// (DirectorLayer.h), и одинаковые имена для «задание из очереди» и «задание с
// ключа --render» путали бы читателя сильнее, чем длинное имя здесь.
struct RenderQueueJob {
    std::string Name;
    SequenceExporter::Settings Settings;
    // Итог прогона: пусто — ещё не выполнялось, "OK" — записано, иначе причина.
    std::string Result;
    bool Done = false;
};

// Очередь заданий. Умышленно тупая: список плюс указатель на текущее. Вся
// работа по-прежнему у SequenceExporter — очередь только решает, какое задание
// запустить следующим, и запоминает, чем кончилось предыдущее.
class RenderQueue {
public:
    std::vector<RenderQueueJob> Jobs;

    bool Running() const { return m_current >= 0; }
    int CurrentIndex() const { return m_current; }
    // Осталось заданий, включая текущее.
    int Remaining() const;

    // Берёт следующее невыполненное задание. false — очередь исчерпана.
    // outSettings — настройки, с которыми надо запускать экспорт.
    bool TakeNext(SequenceExporter::Settings& outSettings);

    // Отмечает текущее задание завершённым. error пуст — успех.
    void FinishCurrent(const std::string& error);

    // Сбрасывает пометки «выполнено» — очередь можно прогнать заново.
    void Reset();
    // Останавливает прогон, не трогая список заданий.
    void Stop() { m_current = -1; }

private:
    int m_current = -1;
};

} // namespace d3d
