# Каталоги сборки.
/build/
/build-*/
/cmake-build-*/
/dist/

# Продукты сборки и логи.
*.o
*.obj
*.exe
*.log
*.ini

# Вывод рендера. Ведущий слэш ОБЯЗАТЕЛЕН: без него шаблон "render/" совпадает с
# каталогом render на ЛЮБОМ уровне — и молча выбрасывает из репозитория
# src/render/ с исходниками превью-рендера и экспорта.
/render/
/smoke_render/

# Среды разработки.
/.vs/
/.vscode/
*.user

# Настройка языка интерфейса (пишется рядом с бинарником).
director3d_lang.txt
